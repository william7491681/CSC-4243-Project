import Card from "./Card"

export default function LandingPage() {

    return (
        <div className="text-center">
            {/* show cards with */}
            <Card>
                Test I suppose
            </Card>
        </div>
    )
}